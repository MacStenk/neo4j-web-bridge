# Server Configuration
PORT=3000

# Neo4j Connection (Optional - can be configured via UI)
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=changeme123
NEO4J_DATABASE=neo4j

# Security Configuration
# API Key required for all API endpoints (generate a strong random string)
API_KEY=your-secret-api-key-here

# CORS Whitelist (comma-separated origins, or * for all - NOT recommended for production)
CORS_ORIGINS=https://mcp-gateway.codeback.de,http://localhost:3001

# Rate Limiting
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=100
